# ChronoCrunch

A neo-brutalist, high-precision temporal progress tracker featuring cyberpunk aesthetics and hard-style animations.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
